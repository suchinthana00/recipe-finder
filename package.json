{
	"name": "recipe-finder",
	"private": true,
	"version": "0.0.1",
	"type": "module",
	"scripts": {
		"copy:stencil-assets": "node scripts/copy-stencil-assets.mjs",
		"predev": "npm run copy:stencil-assets",
		"prebuild": "npm run copy:stencil-assets",
		"dev": "vite dev",
		"build": "vite build",
		"preview": "vite preview",
		"prepare": "svelte-kit sync || echo ''",
		"check": "svelte-kit sync && svelte-check --tsconfig ./tsconfig.json",
		"check:watch": "svelte-kit sync && svelte-check --tsconfig ./tsconfig.json --watch"
	},
	"devDependencies": {
		"@sveltejs/adapter-auto": "^7.0.1",
		"@sveltejs/adapter-cloudflare": "^7.2.9",
		"@sveltejs/kit": "^2.63.0",
		"@sveltejs/vite-plugin-svelte": "^7.1.2",
		"svelte": "^5.56.1",
		"svelte-check": "^4.6.0",
		"typescript": "^6.0.3",
		"vite": "^8.0.16"
	},
	"dependencies": {
		"stencilrecipe": "^0.1.0"
	}
}
